# The AI ESG Reporting Copilot calls the live Claude API through a server-side
# proxy (server/copilot.ts), so this key is never exposed to the browser.
# Copy this file to .env and paste your key.
ANTHROPIC_API_KEY=sk-ant-...
